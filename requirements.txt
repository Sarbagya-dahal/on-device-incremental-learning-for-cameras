# Core (unchanged from Review 1)
streamlit>=1.30.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0

# Phase 2 additions — real camera capture + detection + feature extraction
# All CPU-only; no GPU required.
opencv-python>=4.8.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
